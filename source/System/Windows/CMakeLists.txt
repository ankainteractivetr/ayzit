file(GLOB WINDOWS_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.hpp")

target_sources(${PROJECT_NAME} PRIVATE ${WINDOWS_HEADERS})

target_include_directories(${PROJECT_NAME} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})