file(GLOB RESOURCE_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.hpp")
file(GLOB RESOURCE_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.rc")

target_sources(${PROJECT_NAME} PRIVATE ${RESOURCE_HEADERS} ${RESOURCE_SOURCES})

target_include_directories(${PROJECT_NAME} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})